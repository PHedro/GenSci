def parse(data):
    samples_map = extract_samples(data=data)
    persist_samples(samples_map=samples_map)


def extract_samples(data):
    pass


def parse_sample(sample_data):
    pass


def persist_samples(samples_map):
    pass
